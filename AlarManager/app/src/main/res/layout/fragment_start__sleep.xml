<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/container"
    tools:context=".Start_Sleep"
    android:background="@drawable/start_sleep"
    >

    <TextView
        android:id="@+id/start_stop_text"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="@string/lightCheck"
        tools:layout_editor_absoluteX="142dp"
        tools:layout_editor_absoluteY="236dp" />

    <LinearLayout
        android:id="@+id/stop_layout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_margin="16dp"
        android:animateLayoutChanges="true"
        android:background="@drawable/fab_btn_corner"
        android:gravity="center_vertical"
        app:layout_constraintBottom_toBottomOf="@+id/start_stop_text"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@+id/start_stop_text"
        app:layout_constraintTop_toTopOf="parent">

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/start_stop"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/stop"
            android:src="@drawable/ic_stop"
            app:borderWidth="0dp"
            app:elevation="50dp" />

        <TextView
            android:id="@+id/stop_sleep"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/interbold"
            android:paddingStart="0dp"
            android:paddingEnd="16dp"
            android:text="@string/stop"
            android:textAllCaps="false"
            android:textAppearance="@style/TextAppearance.MaterialComponents.Button"
            android:textColor="@color/colorFabText" />
    </LinearLayout>


</androidx.constraintlayout.widget.ConstraintLayout>